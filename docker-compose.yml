version: "2"

services:
  # db:
  #   image: postgres
  #   environment:
  #     - POSTGRES_DB=postgres
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=postgres
  web:
    build:
      context: .
      dockerfile: ./compose/Dockerfile.prod
    container_name: audiovyvy_c
    command: python manage.py runserver 0.0.0.0:8000
    # volumes:
    #   - .:/code
    environment:
      - MODE_ENV=production
    ports:
      - "8000:8000"
    # depends_on:
    #   - db
